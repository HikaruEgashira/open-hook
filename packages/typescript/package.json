{
  "name": "@openhook/sdk",
  "version": "0.1.0",
  "description": "OpenHook Protocol SDK — standardized hook payloads for AI coding agents",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
